{
  "name": "stuff",
  "version": "1.0.0",
  "private": true,
  "description": "Todoist as the front-end for a Notion tasks database, synced two-way by a Notion Worker.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "check": "npm --prefix apps/sync-worker run check",
    "build": "npm --prefix apps/sync-worker run build"
  }
}
